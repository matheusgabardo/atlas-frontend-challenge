// Human-readable labels for price models (pt-BR UI copy).

import type { PriceModel } from '../types/professional'

export const PRICE_MODEL_LABEL: Record<PriceModel, string> = {
  evento: 'evento',
  diaria: 'diária',
  hora: 'hora',
}
